name: Check Links

on:
    push:
        branches:
            - '**'

jobs:
  link_checker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Link Checker
        uses: lycheeverse/lychee-action@v2
        with:
            fail: true
            args: >
                --exclude ImplementationGuide/style
              
       
